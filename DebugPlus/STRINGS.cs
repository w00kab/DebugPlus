namespace DebugPlus
{
    /// <summary>
    /// Mod 字符串定义占位（P0 骨架预留）。
    /// 约定：本地化类必须命名为 STRINGS（类名不同会破坏 LocString 键路径，oni-mod-dev 技能规则）。
    /// 结构：嵌套 class 层级对应 key 路径（STRINGS.UI.xxx / STRINGS.BUILDINGS.PREFABS.xxx 等）。
    /// 翻译体系（LocalisationUtil / i18n 编译）与具体字符串在 P1 导入基底、需要游戏内文本时按 M4 建立。
    /// </summary>
    internal class STRINGS
    {
        // 暂无字符串条目（翻译另置，见 plan.md M4）。
    }
}
