本目录存放 DebugPlus 的图标资源说明。当前版本**无任何资源文件**（不携带、不生成图片），
仅保留此说明。

合规约定（plan.md 二.6）：
- 图标一律自绘或复用游戏自带资源，不采用他人仓库的图片资源
  （含 CC BY-NC-SA 等带署名 / 非商业 / 同许可共享义务的资源）；
- 复用游戏自带资源不承担额外署名义务。

沿革：
- 早期 P1 曾存在 `filtered_destroy.png`（128×128 程序化占位图，随 csproj EmbeddedResource
  内嵌），后改为纯代码运行时绘制（`UI\SpriteRegistry.cs`，逻辑名 `filtered_destroy`），
  再改为直接复用游戏原版 `destroy` 图标并删除前两套方案。
- 2026 去上游化清理：上述图标方案所服务的"分类清除工具"属**另一 Mod 的功能领域**
  （plan.md 二.1），该工具及全部相关文件已整体删除，故本目录的方案**回到未定**。
- 图标方案待 M1（用户菜单按钮 / 模态弹窗）真正需要时再定：优先复用游戏自带图标，其次自绘。
